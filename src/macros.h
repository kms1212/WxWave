#ifndef __MACROS_H__
#define __MACROS_H__

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#define ARRAY_SIZE(arr) (sizeof(arr) / sizeof(*(arr)))

#endif // __MACROS_H__
